<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title>File Uploader</title>
  </head>
  <body>
    <%- include('partials/leftAside.ejs') %>
    <main class="indexContainer">
      <div>
        <% if(locals.currentUser){ %>
        <p>Welcome <%=currentUser.username %></p>
        <% }else{ %>
        <p>Welcome to File Uploader!</p>
        <% } %>
        <p>🚀 Simple, Fast & Secure File Uploads</p>
        <p>⬆️ Drag & drop files</p>
        <p>📎 Click to browse your device</p>
        <p>✅ Supports: Images, Documents, Videos, and more</p>
        <p>
          <%= locals.currentUser? 'New Features':'Start By Creating An Account'
          %>
        </p>
      </div>
    </main>
    <%- include('partials/rightAside.ejs') %>
  </body>
</html>
